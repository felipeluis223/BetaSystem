// Payload para criar os inputs de acordo com a tabela:
type User = {
    name: string;
    email: string;
    password: string;
};

type Employee = {
    name: string;
    cpf: string;
    rg: string;
    email: string;
    phone: string; 
}